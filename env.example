# Server Configuration
PORT=3001
HOST=0.0.0.0

# Client Origins (comma-separated)
CLIENT_ORIGIN=http://localhost:3000,http://192.168.1.13:3000

# WebRTC to RTMP Bridge Configuration

# Mux RTMP URL
MUX_RTMP_URL=rtmp://global-live.mux.com:5222/app

# TURN Server Configuration (optional but recommended for NAT traversal)
# Format: turn:hostname:port or turns:hostname:port (for TLS)
# Multiple servers can be comma-separated
TURN_SERVER_URL=turn:your-turn-server.com:3478

# TURN Server Credentials (if required)
TURN_USERNAME=your-turn-username
TURN_CREDENTIAL=your-turn-password

# FFmpeg Configuration (optional)
# If FFmpeg is not in PATH, specify full path
# FFMPEG_PATH=/usr/bin/ffmpeg
